import '../../styles/loginAndRegistration.scss'

// Importação de dependências
import { useFormik } from 'formik'
import * as yup from 'yup'

export function SupplierRegistration() {

    return (
        <form noValidate>
            <p>Cadastro como fornecedor</p>
            
        </form>
    )
}